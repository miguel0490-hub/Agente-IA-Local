# Locust configuration for staging load tests
# Usage: locust -f tests/load/locustfile.py --config tests/load/locust.conf

host = http://localhost
users = 20
spawn-rate = 5
run-time = 2m
headless = false
